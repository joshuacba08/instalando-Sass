{
  "name": "instalando-sass",
  "version": "1.0.0",
  "description": "Proyecto para tener una estructura con Sass",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass":"node-sass sass/main.scss css/style.css --watch"
  },
  "author": "Josue Oroya",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.1"
  }
}
